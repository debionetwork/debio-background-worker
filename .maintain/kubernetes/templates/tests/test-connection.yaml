apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "debio-indexer.fullname" . }}-test-connection"
  labels:
    {{- include "debio-indexer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "debio-indexer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
