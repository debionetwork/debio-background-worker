export enum OrderFlow {
    RequestTest = "Request Test", // not sure, change after types.json update
    StakingRequestService = "Staking Request Service" // not sure, change after types.json update
}